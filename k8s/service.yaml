apiVersion: v1
kind: Service
metadata:
  name: kube-reflex-service
  labels:
    app: kube-reflex
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
  selector:
    app: kube-reflex
