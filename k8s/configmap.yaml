apiVersion: v1
kind: ConfigMap
metadata:
  name: kube-reflex-config
  labels:
    app: kube-reflex
data:
  # Redis address (use your Redis service name; optional - default in code is localhost:6379)
  redis-addr: "redis:6379"
